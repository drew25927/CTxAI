# 🛠 개발 이어가기 — 새 세션용 인수인계

**새 Claude Code 세션(다른 창)에서 이 문서를 열어 시작하시면 됩니다.**
**작성:** 개발 · 2026-08-08 · **갱신:** 개발(세션) · 2026-09-14

---

## 0. 지금 뭐가 있는지

| | |
|---|---|
| 문서 저장소 | `/Users/drew/CTxAI/Bus/규격/` |
| 앱 저장소 | `/Users/drew/CTxAI/ctxai-busstop-xr-audio-proto/` |
| git 원격 | `github.com/drew25927/CTxAI` (모노레포 — Bus/, ctxai-busstop-xr-audio-proto/ 전부 한 저장소. 이 외 것들은 §1 참고) |
| 배포 주소 | **https://busstop-team.vercel.app** (막히면 **https://busstop-team-live.vercel.app** — §1 참고) |
| Vercel 프로젝트 | `drew-b295/busstop-team` |
| Supabase 프로젝트 | `busstop` (ap-northeast-2) — 파일·프리셋·할일·대사오디오·SFX 후보 전부 여기 저장 |
| 팀 온보딩(비개발자용) | `/Users/drew/CTxAI/ONBOARDING.md` — 이 문서와 별개, 2026-09-10 기준이라 `/interim` 반영 안 됨. 이 문서(개발_이어가기.md)가 더 최신 |

로그인 없음. 로컬 dev 서버와 프로덕션이 **같은 Supabase**를 보므로, 로컬에서 저장한 것도 배포 없이 바로 프로덕션에 반영됩니다(코드 변경만 배포가 필요).

**체험 화면 — 어느 것을 열어야 하나**

| 주소 | 무엇 | 상태 |
|---|---|---|
| `/interim` | **2분 20초 중간시연 MVP.** 헤드셋/드래그 머리 방향 + 웹캠 표정 + 마이크로 5신호(S1~S5) 판정, 로맨스·공포·블랙코미디 확정 | ⭐ 2026-09-14 신규 · 11/7 발표용 파일럿 대상 |
| `/film` | **반응형 실시간 영화(5분).** 판정 1회가 아니라 관객 반응이 체험 내내 하늘·빛·안개·가로등·옆사람 거리/시선·대사 간격·BGM을 계속 움직임. 헤드셋(WebXR)·데스크톱 드래그 모두 지원 | 2026-09-10 1차 구현, 파일럿 전 |
| `/story-vr` | 관찰 11초 → 판정 1회 → 고정 장면. 3D 그레이박스 + WebXR | 이전 버전(실험) |
| `/story-v2` | 위와 같은 판정에 2D 원화 배경, 배합 비율 그대로 표시 | 이전 버전 |
| `/story` | 장르 하나를 확정하는 v1 | 구버전 |

**팀이 자주 쓰는 그 외 페이지**
- `/todo` (= `/`, 루트가 자동 리다이렉트) — 할 일 목록 + 진행 상황 + 발표 자료 링크 모음. **실제 홈페이지**
- `/upload` — 파일 업로드 대시보드 (메뉴에서 숨김, 직접 주소로만 접근)
- `/vo` — 대사 오디오 46줄 듣기 (`?genre=R` 필터), `/sfx` — 환경 SFX 후보 17종 듣기
- `/whitebox` — 3D 화이트박스 + 조명 프리셋 편집 + VR 진입 (개발자용)
- `/judge` (v1) / `/judge-v2` (v2) — 판정 점수 실시간 확인 (개발자용)
- `/guide?doc=정류장_스크립트_v2` 등 — `Bus/규격/*.md` 문서를 사이트에서 그대로 보는 뷰어
- `/onepager.html` — 중간 발표 원페이지 (정적 HTML, React 아님)

---

## 1. 이 세션을 시작하기 전에 — 반드시 확인할 것

**이 저장소는 동시에 여러 Claude Code 세션에서 건드릴 수 있습니다.**

```bash
cd /Users/drew/CTxAI
git status
git log --oneline -15
git pull origin main
curl -s https://busstop-team.vercel.app/api/tasks   # 할 일 목록 — 다른 창에서 지웠거나 추가했을 수 있음
```

⚠️ **아래는 이 프로젝트 것이 아닙니다. 절대 건드리거나 커밋하지 마세요:**
- `/Users/drew/CTxAI/CLAUDE.md`, `WORLD.md`, `myfortune/` — 완전히 다른 창작 세션 파일
- `/Users/drew/CTxAI/data/`(384MB), `assets/`(293MB), `ctai_xr_blocks/`(445MB), `ct_ai_office1_apartment1_assets.zip`, `STUDENT_ASSETS.json`, `VSCode-darwin-arm64.dmg` — 루트 `.gitignore`에 안 걸려 있는 **큰 미확인 자산 덩어리**(다른 XR 블록/과제 스캐폴드로 추정). `git add -A`나 `git add .`를 쓰면 통째로 스테이징될 수 있으니, **커밋할 땐 항상 파일을 명시해서 add 하세요.**

**✅ 2026-09-14 확인: 앱 코드(`ctxai-busstop-xr-audio-proto/`)는 현재 전부 커밋된 상태입니다.** (예전 버전 이 문서에 있던 "배포했지만 커밋 안 한 코드" 경고는 해소됨 — `/vo`, `/sfx` 포함 전부 git에 있습니다.)

### Vercel이 통째로 막히면 (x-vercel-mitigated: deny, 403)

프로덕션 도메인(`busstop-team.vercel.app`)이 Vercel의 **자동 트래픽 완화(플랫폼 레벨 봇/공격 차단)**로 전체 차단된 적이 있었습니다(2026-09 초). Deployment Protection(SSO)과는 다른 메커니즘이라 `vercel.json`으로 못 끕니다. 응답 헤더에 `x-vercel-mitigated: deny`가 있으면 이 문제입니다.

**해결/우회 (이미 적용해서 유지 중 — 사용자 지시로 되돌리지 않음):**
1. `drew-b295/busstop-team` 프로젝트의 `ssoProtection`을 Vercel REST API로 꺼둠 (`PATCH https://api.vercel.com/v9/projects/busstop-team`)
2. 예비 별칭 `busstop-team-live.vercel.app`을 만들어서 **배포 ID가 아니라 프로덕션 도메인 자체**에 alias 해둠(`vercel alias set busstop-team.vercel.app busstop-team-live.vercel.app`) — 이렇게 해야 이후 배포마다 자동으로 최신을 따라감. 주 도메인이 다시 막히면 이 주소로 안내하세요.

---

## 2. 스토리 구조 — 판정기 → 실시간 연출 엔진으로

**`Bus/규격/정류장_스크립트_v2.md`가 여전히 확정 대사·판정 규칙 문서입니다.** 그 위에 두 갈래로 발전했습니다.

| | v1 (구) | v2 (연속 블렌딩) | `/film` (반응형 실시간 영화) | `/interim` (중간시연 MVP) |
|---|---|---|---|---|
| 장르 수 | 4개(H·R·C·F) | 3개(공포·로맨스·블랙코미디, 판타지 드롭 확정 — `lib/assetSpec.js`의 `GENRES = ["H","R","C"]`) | 3개 | 3개 |
| 판정 방식 | 관객이 장르 선택 | 관찰 11초 → 1회 판정 → 배합 비율 고정 표시 | **판정을 1회로 끝내지 않음** — 연출 상태가 체험 5분 내내 증거를 누적·완만 추종 | 5개 신호(S1~S5)를 스펙 점수표대로 판정, 2분 20초 |
| 센서 | 없음(선택) | 웹캠 표정(behaviorSense.js) + STT/LLM + 음성 톤 | 헤드 포즈 1순위 + 웹캠 보조 + 음성(선택) | 헤드셋/드래그 머리 방향 + 웹캠 표정 + 마이크 |
| 무엇이 바뀌는가 | 장면 1개 | 그림 1장 + BGM 볼륨 | 하늘·태양·안개·가로등·도로반사·옆사람 거리/시선·대사 간격·BGM 전부 | 5신호 결과로 배경·NPC·BGM 확정 |
| 설계 문서 | `Bus/7조_버스정류장.md` | `Bus/규격/기술_발전_방향.md`, `판정_기준.md` | `Bus/규격/반응형_실시간_영화.md` | `Bus/인수인계_중간시연_interim.md` |

**`/film`과 `/interim`은 서로 다른 라우트라 완전히 독립적입니다** — 하나를 고쳐도 다른 하나에 영향 없습니다. 둘 다 `/todo`에서 링크로 연결되어 있고, 원래 `/story-v2`/`/story-vr`가 하던 "웹캠+음성 판정" 역할을 각자 다른 방향(하나는 "판정 없는 연속 연출", 하나는 "짧고 명확한 파일럿 데모")으로 이어받은 것입니다. **`/story`, `/story-v2`, `/story-vr`, `/judge`, `/judge-v2`는 손대지 않고 실험/이전 버전으로 남아 있습니다.**

### 아직 안 풀린 것
- **`/film` vs `/interim` 중 11/7 발표에서 뭘 메인으로 쓸지** — 아직 팀 결정 안 남음. `/interim`은 짧고 판정이 명확해서 심사에 유리하고, `/film`은 "연속 블렌딩"이라는 원 제안서 핵심 주장을 훨씬 더 잘 보여줍니다. 둘 다 살아있으니 리허설해보고 정하면 됩니다.
- **행동 판정 임계값** — 둘 다 파일럿 테스트 전 잠정치. §4 참고.

---

## 3. 사운드 · 아트 자산 — 대부분 끝남, 파일럿 대기

| 항목 | 상태 |
|---|---|
| 대사 46줄 TTS(로맨스14·공포14·블랙코미디18) | ✅ `/vo`에서 재생 가능. `/film`은 이걸 `public/reactive/audio/`로 로컬 복사해서 씀(`npm run pull-assets`) |
| 대사 사전 생성 풀(원문 변주 2종, `?pool=1`) | ✅ 1차 구현 — 남은 건 기획 검수 |
| 환경 SFX | `/film`용 18종은 로컬에 있음. `/sfx` 페이지의 17종 후보는 여전히 최종 선택 대기 |
| BGM | 4트랙(H/R/C + 구 판타지) 정규화 완료 |
| 3D 배경(HDRI·PBR 텍스처·정류장/버스/트럭 지오메트리) | ✅ `/film`용으로 2026-09-10~11에 새로 만듦(`public/reactive/hdri/`, `textures/`, `models/`) — 회색 상자에서 실사 수준으로 올라감 |
| NPC 캐릭터 리깅 | 임시(Meshy 리깅, 걷기 클립만 있어 앉는 자세는 코드로 흉내냄). 변환 스크립트(`scripts/blender/mixamo_to_glb.py`)는 준비돼 있지만, **Blender는 이 macOS 환경에 설치돼 있지 않음** — Mixamo(Adobe 계정)에서 FBX를 받은 뒤 Blender 설치 여부를 먼저 사용자와 상의할 것. §7 참고 |
| `/interim` 임계값(놀람/웃음 민감도, 기립 인정 기준) | ⏳ 전부 잠정치, 실측 보정 필요 — `Bus/인수인계_중간시연_interim.md` §4 참고 |

---

## 4. 판정 로직이 사는 곳

- `lib/behaviorSense.js` — `/story-v2`·`/story-vr`가 쓰는 원래 판정. 웹캠 표정(MediaPipe FaceLandmarker 블렌드셰이프) 기반. **AND 조건을 average가 아니라 `Math.min()`(퍼지 AND)으로 합성** — 로맨스처럼 조용한 반응이 공포로 오판되던 버그를 이렇게 고쳤음(`판정_기준.md` 참고). 새 판정 로직을 짤 때 이 패턴(단순 평균 대신 조건별 min/max 합성)을 그대로 따르는 게 안전함.
- `lib/directionState.js` + `lib/directionMap.js` + `lib/headPoseSense.js` — `/film`의 연출 상태. 헤드 포즈가 1순위, 웹캠은 보조. §2의 표대로 매 프레임 갱신.
- `lib/interimJudge.js` + `lib/interimGrader.js` + `lib/interimMic.js` + `lib/standUpSense.js` — `/interim`의 5신호(S1~S5) 판정. 스펙(`기다림_정류장_프로젝트개요서.pdf`, `기다림_정류장_XR_중간시연_구현가이드_v1.0.docx`) 점수표를 그대로 구현, 12케이스 단위 테스트 있음.

세 판정기는 서로 코드를 공유하지 않는 완전히 독립된 구현입니다 (의도적 — 각자 다른 라우트가 다른 실험이라서).

---

## 5. 앱 구조 (2026-09-14 기준)

```
ctxai-busstop-xr-audio-proto/
├── app/
│   ├── page.js                루트 — /todo로 redirect
│   ├── todo/                  ⭐ 실제 홈. 할 일 · 진행 상황 · 개발 중인 화면 · 발표 자료
│   ├── upload/                필수 에셋 업로드 대시보드 (메뉴에서 숨김)
│   ├── film/, film/compare/   ⭐ 반응형 실시간 영화 + 두 세션 비교
│   ├── interim/                ⭐ 2분 20초 중간시연 MVP
│   ├── story/, story-v2/, story-vr/   관객용 체험 데모 3종 (구버전/실험 포함)
│   ├── judge/, judge-v2/      판정 대시보드 (개발자용)
│   ├── vo/, sfx/              대사·SFX 청취 페이지
│   ├── whitebox/              3D 화이트박스(react-three-fiber + @react-three/xr)
│   ├── tool/, verify/, selftest/, guide/, demo/   점검·문서뷰어·기존 실험
│   └── api/
│       ├── dialogue/, tts/, meshy/, session/   ⭐ /film용 — 실시간 대사 생성, 합성음, 세션 기록
│       ├── mood/, voicetone/    STT+LLM 채점, 음성 톤 분석 (OpenRouter)
│       ├── vo2/, sfx/           대사·SFX 업로드/서빙
│       ├── assets/, preset/, tasks/, manifest/, health/   기존 그대로
├── lib/
│   ├── behaviorSense.js       story-v2/vr 판정 (웹캠 블렌드셰이프)
│   ├── directionState.js, directionMap.js, headPoseSense.js, filmTimeline.js, dialogueBeats.js, dialoguePool.js   ⭐ /film 엔진
│   ├── interimJudge.js, interimGrader.js, interimMic.js, interimTimeline.js, interimDrift.js, standUpSense.js   ⭐ /interim 판정
│   ├── lightingBlend.js, bgmBlend.js, moodMix.js   조명/BGM 배합 (구/신 버전 공존)
│   ├── assetSpec.js, dialogueV2Lines.js, sfxCandidates.js, dashboardData.js, scriptTasks.js, milestones.generated.json   대시보드·자산 정의
│   └── measure/, store.js, manifestBuild.js   기존 그대로
├── components/
│   ├── ReactiveStage.jsx, Bus.jsx, Truck.jsx, Puddles.jsx   ⭐ /film 3D 무대
│   └── (기존 컴포넌트들)
├── scripts/
│   ├── pull-assets.mjs         Supabase 오디오 → public/reactive/audio/
│   ├── gen-dialogue-pool.mjs   대사 변주 사전 생성
│   ├── gen-milestones.mjs      git 로그 → 대시보드 "진행 상황"
│   ├── test-direction.mjs, sim-headpose.mjs   회귀 테스트 (npm test)
│   ├── observe.sh, cdp.mjs, assemble-recording.sh   헤드리스 크롬 관찰·녹화
│   └── blender/mixamo_to_glb.py   Mixamo FBX → GLB 병합 (Blender 헤드리스)
├── public/reactive/           /film 전용 로컬 에셋 — audio 20MB · hdri 13MB · models 43MB · textures 10MB
└── data/sessions/             /film 세션 기록 (gitignore 대상)
```

---

## 6. 로컬 개발 · 배포

```bash
cd /Users/drew/CTxAI/ctxai-busstop-xr-audio-proto
npm install
npm run dev          # 일반 페이지 (story, judge, interim 등)
npm run film         # /film 전용 — 셸에 NODE_ENV·TURBOPACK이 잡혀 있어도 안전하게 next dev
npm run pull-assets  # /film용 오디오를 Supabase에서 로컬로 (최초 1회)
```

빌드: `npm run build:clean` (film과 같은 이유로 env 정리 후 build). 회귀 테스트: `npm test` (`test-direction.mjs` + `sim-headpose.mjs`).

배포:
```bash
npm run milestones   # 대시보드 "진행 상황"에 최근 커밋 반영 (배포 전에 매번)
TOK=$(cat ../.vercel-token | tr -d '[:space:]')   # 또는 앱 폴더 안에 있으면 그 경로
npx --yes vercel deploy --prod --token="$TOK" --yes
```

**이 macOS 환경에 없는 것들:**
- `ffmpeg`, `lame`, `sox` — 없음. wav→mp3 인코드 불가(Apple 라이선스). `afconvert`/`afinfo`(macOS 내장)로 디코드만 가능.
- `blender` — **여전히 없음** (2026-09-14 재확인 — 전에 설치됐었다는 기록이 있었으나 지금 이 환경엔 없음). 코드 기반 3D 모델링/변환(Mixamo GLB 병합 등)을 하려면 먼저 설치 여부를 사용자와 상의할 것.
- `python-pptx`는 pip로 설치 가능(3.9.6 환경에서도 동작). `validate.py`/`soffice.py` 같은 pptx 스킬의 검증 도구는 Python 3.10+ 문법을 써서 이 환경에서 `SyntaxError`로 실패함 — pptx를 직접 다뤄야 하면 `python-pptx`로 위치 매칭(`Emu(shape.top).inches` 등) 후 `run.text`만 고치는 방식으로 우회.

---

## 7. 지금까지 배운 것 (같은 실수 반복 방지)

### 기존 (v1.1 시절부터)
- Supabase Storage 새 형식 키(`sb_secret_...`)는 `apikey` 헤더도 같이 보내야 인증됨
- Supabase Storage 객체 키에 비-ASCII 금지 — `slotId+확장자`로 고정, 원본 파일명은 `records.json`에만
- `@react-three/fiber@8.18.0` + `@react-three/drei@9.122.0` + `three@0.183.0` 고정 (React 18/Next 14 호환)
- 조명 프리셋·할일·대사오디오·SFX는 파일이 아니라 Supabase에 저장됨 — 테스트로 올리면 팀 전체가 보는 실데이터가 됨
- `/api/assets` 슬롯은 **맨 처음 업로드만 자동 선택**됨, 이후는 `/api/assets/choose`를 명시 호출해야 함
- Vercel 서버리스 함수는 요청 바디 ~4.5MB 넘으면 413으로 거부 — 큰 오디오는 모노 다운믹스 등으로 우회

### `/film`·`/interim` 작업하며 새로 배운 것 (2026-09)
- **MediaPipe 판정에서 얼굴 랜드마크 하나(`CHIN`)를 기준점으로 쓰면 손으로 입을 가리는 것만으로 판정이 깨짐** — 대신 잘 안 가려지는 지점(`NOSE_BRIDGE` 등)을 쓰고, 프레임 간 이상치(순간적으로 튀는 값)를 버리는 로직을 넣을 것.
- **표정 블렌드셰이프 중 `jawOpen`은 웃음과 공포(입 벌림)에 둘 다 걸림** — 감정별 블렌드셰이프 세트를 겹치지 않게 고를 것. 겹치면 교차 억제항(한쪽이 강하면 다른 쪽 신호를 깎는 식)을 넣어야 함.
- **여러 조건의 AND를 평균(`(a+b+c)/3`)으로 구현하면 안 됨** — 신호 두 개만 강해도 세 번째가 0인데 평균이 높게 나와 오판함. `Math.min()`으로 진짜 AND를 구현할 것 (`lib/behaviorSense.js`가 실제 사례).
- **Vercel의 "자동 트래픽 완화"(`x-vercel-mitigated: deny`)는 Deployment Protection(SSO)과 다른, 플랫폼 레벨 차단**이라 프로젝트 설정만으로는 못 끔. §1의 우회(별칭 도메인 + `ssoProtection` API로 끄기)를 참고.
- **`npm run film`이 필요한 이유**: 이 개발 환경 셸에 비표준 `NODE_ENV`/`TURBOPACK`이 이미 잡혀 있어서, 그냥 `next dev`/`next build`를 돌리면 CSS 로드나 빌드가 깨짐. `env -u NODE_ENV -u TURBOPACK`으로 지운 뒤 실행하는 래퍼가 그래서 있음 — 다른 스크립트를 새로 추가할 때도 이 패턴을 따를 것.
- **헤드리스 크롬으로 `/film`을 관찰할 때 탭을 두 개 이상 띄우면 fps가 떨어져 프레임 dt가 0.1초로 클램프되고, 영화 시간 자체가 실제보다 느려짐** — 한 번에 탭 하나만.
- Blender는 이 환경에 설치돼 있지 않다(§6) — 설치됐었다는 이전 기록과 달리 2026-09-14 확인 결과 없음. 3D 모델링/변환 작업을 코드로 해야 하면 설치부터 사용자와 상의할 것.
- 신뢰 안 되는 사이트에서 바이너리를 받아 실행하는 대신, 있는 도구(macOS 내장, 순수 JS/Python)로 우회하는 판단 기준 유지.

---

## 8. 다음에 할 일 (우선순위)

1. **`/film` vs `/interim` 중 11/7 메인 데모 결정** — 리허설해보고 팀이 정할 것 (§2 참고).
2. **`/interim` 임계값 실측 보정** — `Bus/인수인계_중간시연_interim.md` §4-(1)에 정확한 파일·숫자 목록 있음. 코드는 안 바꿔도 되고 숫자만 몇 번 실측 후 고치면 됨.
3. **헤드셋 실측** (`/film` §7-1) — Quest 3에서 헤드 포즈 임계값(±28° 응시, 140°/s 화들짝, 7cm 후퇴) 보정. 데스크톱 드래그로는 후퇴 신호 자체가 없음.
4. **파일럿 10~20명** — 목표 11/7 발표 1주 전. `/film`은 종료 카드 자기보고가, `/interim`은 HUD 정직 표시가 이미 있어서 바로 돌릴 수 있음.
5. **Mixamo 인물 리깅** (§3) — Adobe 계정으로 Mixamo 로그인 → FBX 3종(인물+동작) 다운로드만 하면, 변환 스크립트는 이미 준비돼 있음.
6. **`/sfx` 최종 SFX 선택** — 사운드 담당이 17개 후보 중 직접 들어보고 고르는 일.
7. **대사 변주 풀 검수** — `?pool=1`로 생성된 보조 장르 변주 텍스트를 기획이 읽고 고치는 일 (§3).
8. **불필요한 대용량 파일 정리 여부 확인** — §1의 `data/`·`assets/`·`ctai_xr_blocks/`가 정말 이 프로젝트와 무관한지 사용자에게 재확인하고, 무관하면 삭제(또는 최소한 `.gitignore`에 추가)할지 결정.

---

## 9. 참고 문서 목록 (`Bus/규격/`, 최신순 정리)

| 파일 | 내용 |
|---|---|
| `반응형_실시간_영화.md` | ⭐ `/film` 설계 — 연출 상태·매핑표·센서·타임라인·남은 일 (2026-09-11 갱신) |
| `데모_시나리오_반응형영화.md` | `/film` 발표·전시 리허설 가이드 (11/7용) |
| `3D_배경_구성_기획.md` | `/film`·`/story-vr` 3D 배경 좌표/구성 원칙 (지형 고정, 스크립트 근거) |
| `Bus/인수인계_중간시연_interim.md` | ⭐ (Bus/ 바로 밑, 규격/ 아님) `/interim` 설계·상태·임계값 목록·파일럿 전 할 일 |
| `기술_발전_방향.md`, `판정_기준.md`, `구현_리스크와_지원_필요사항.md`, `방향전환_업무재분장.md` | 8월 말 "연속 블렌딩 채택" 결정 당시 작성된 4종 세트. 지금도 유효하지만 `/film`·`/interim`이 그 이후 구현 |
| `정류장_스크립트_v2.md` | ⭐ 확정 스크립트 — 공통 도입부·판정 규칙·3트랙 대사 전체 |
| `정류장_스크립트_v1.1.md` | 로맨스 트랙 원본 |
| `대사양식_v2.csv` | 대사 46줄 실제 표 — `lib/dialogueV2Lines.js`와 짝 |
| `SFX_후보_목록.md` | 환경 SFX 17종 스톡 후보 + 라이선스 |
| `명명규칙.md` | 파일명·GLB 규약 (아직 구버전 4장르 기준 — 참고만) |
| `manifest.schema.json`, `validate-manifest.mjs`, `Q2_초안.md`, `preset/`, `대시보드_사용설명서.md`, `사운드_작업_착수.md` | 기존 그대로 |
| `/Users/drew/CTxAI/ONBOARDING.md` | 비개발자 팀원용 온보딩 (2026-09-10 기준, `/interim` 반영 안 됨) |
| `Bus/7조_버스정류장.md` | 원 제안서 — 핵심기술(연속 파라미터 블렌딩) 정의, 모든 방향 판단의 기준점 |

---

**막히면 이 문서부터 다시 읽고, `git log`·`git status`·`/api/tasks`로 최근 변경을 확인하세요.** 질문 없으셔도 됩니다.
